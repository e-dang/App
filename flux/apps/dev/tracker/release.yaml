apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tracker
  namespace: default
spec:
  postRenderers:
    - kustomize:
        images:
          - name: ericdang/tracker
            newName: ericdang/tracker
            newTag: staging-1ea6099f-1626832548 # {"$imagepolicy": "flux-system:tracker:tag"}
