apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: tracker
  namespace: default
spec:
  values:
    ingress:
      annotations:
        cert-manager.io/issuer: letsencrypt
      hosts:
        - host: dev.erickdang.com
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - dev.erickdang.com
          secretName: letsencrypt-tracker
  postRenderers:
    - kustomize:
        images:
          - name: ericdang/tracker
            newName: ericdang/tracker
            newTag: dev-17680bdb-1632089910 # {"$imagepolicy": "flux-system:tracker:tag"}
